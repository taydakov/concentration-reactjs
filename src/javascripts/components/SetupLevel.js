/* Static dependencies */

/* JS dependencies */
import React from 'react';

export default class SetupLevel extends React.Component {

	constructor (...args) {
		super(...args);

		this.state = {
		}
	};

	render () {
		return (
			<h1>This is SetupLevel page</h1>
		);
	}

};

SetupLevel.propTypes = {
};